function changeTemperature(value:number,toUnit:string,fromUnit:number):number{if (fromUnit .toLowerCase()==='c'||fromUnit.toLowerCase()==='celseius')if (toUnit.toLowerCase()) {
    
} {
    
}}
}
    
}