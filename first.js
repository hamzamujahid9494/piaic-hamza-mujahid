"use strict";
function changeTemperature(value, toUnit, fromUnit) {
    if (fromUnit.toLowerCase() === 'c' || fromUnit.toLowerCase() === 'celseius')
        if (toUnit.toLowerCase()) {
        }
    {
    }
}
